<Window x:Class="LPTUnoApp.RecipientsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurar Emails e Destinatários" Height="450" Width="600"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Configuração Google Apps Script" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Script URL:" VerticalAlignment="Center" Margin="0,5,10,5"/>
            <TextBox x:Name="TxtScriptUrl" Grid.Column="1" Margin="0,5" Padding="5"/>

            <TextBlock Text="Nome Remetente:" Grid.Row="1" VerticalAlignment="Center" Margin="0,5,10,5"/>
            <TextBox x:Name="TxtSenderName" Grid.Row="1" Grid.Column="1" Margin="0,5" Padding="5"/>
        </Grid>

        <TextBlock Grid.Row="2" Text="Destinatários (Se o nome aparecer no arquivo, o email será enviado)" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="2" Text="Use vírgula para múltiplos emails." FontStyle="Italic" FontSize="12" Margin="0,0,0,10" HorizontalAlignment="Right"/>

        <DataGrid x:Name="GridRecipients" Grid.Row="3" AutoGenerateColumns="False" CanUserAddRows="True" Margin="0,0,0,20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome a Procurar" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Email de Destino" Binding="{Binding Email}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnCancel" Content="Cancelar" Width="100" Margin="0,0,10,0" Padding="5" Click="BtnCancel_Click"/>
            <Button x:Name="BtnSave" Content="Salvar" Width="100" Padding="5" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
